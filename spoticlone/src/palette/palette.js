import { createTheme } from "@mui/material/styles";

const theme = createTheme({
    palette: {
      primary: {
        main: '#212529',
      },
      secondary: {
        main: '#198754',
        light: '#FFFFFF',
        mainLight: '#FFFFFF',
        grey: '#aaaaaa'
      },
    },
  
});

export default theme;