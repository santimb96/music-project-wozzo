import { createTheme } from "@mui/material/styles";

const theme = createTheme({
    palette: {
      primary: {
        main: '#17191c',
      },
      secondary: {
        main: '#159643',
        light: '#FFFFFF',
      },
    },
  
});

export default theme;